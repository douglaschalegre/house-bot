services:
  house-bot:
    container_name: house-bot
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    init: true
    env_file:
      - .env
    environment:
      TZ: America/Sao_Paulo
      SHOPPING_DB_PATH: /app/data/shopping_list.sqlite3
    volumes:
      - ./credentials.json:/app/credentials.json:ro
      - ./data:/app/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
